会议记录

问题：
1. 手动合到master分支，多人开发导致代码混乱
2. 测试和线上的环境变量，需要手动去改配置 
3. 分支太多，需要出一份分支管理规范

解决：
1. webpack 配置，不同的环境变量，生成配置代码
2. 测试环境 隔离，master 和 若干个分支，修改测试环境指向，每次只指向一个
3. 当前分支上 手动打tag，发布的时候，需要通过不同的指令
4. 代码自动合到master


webpack 配置
npm 指令

开发分支 
开发分支

jenkins 选分支 

dev分支 替换成 开发分支

在 dev 分支 打tag 手动

通过环境变量 和 指令 去生成线上 

线上回滚






